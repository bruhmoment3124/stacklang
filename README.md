## keywords
### stack manipulation:

``` push ```:

``` pop ```:

``` swp ```:

``` cmp ```: 

``` top ```:

### blocks:

``` sub ```:

``` loop ```:

``` cond ```:

``` begin ```:

``` end ```:

``` jmp ```:

### I/O:

``` put ```:

``` take ```:
